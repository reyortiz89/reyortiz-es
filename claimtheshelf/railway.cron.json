{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo cron service: nothing to build"
  },
  "deploy": {
    "startCommand": "node scripts/cron.mjs",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
